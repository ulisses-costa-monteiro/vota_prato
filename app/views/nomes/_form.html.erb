<%= form_for(@nome) do |f| %>
  <% if @nome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nome.errors.count, "error") %> prohibited this nome from being saved:</h2>

      <ul>
      <% @nome.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
